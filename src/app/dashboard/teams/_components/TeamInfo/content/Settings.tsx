import { ContentProps, INITIAL_EDIT_MODES } from "../info";

export function Settings({
  darkMode,
  team,
  // setEditMode,
  // editMode = INITIAL_EDIT_MODES,
  // activeTab = "NameAndLeague",
}: ContentProps) {
  return <div>Settings</div>;
}
